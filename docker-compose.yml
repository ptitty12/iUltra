services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - DB_PATH=/data/drinks.db
    volumes:
      - sqlite_data:/data
    networks:
      - dokploy-network

volumes:
  sqlite_data:

networks:
  dokploy-network:
    external: true