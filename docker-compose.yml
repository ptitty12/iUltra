services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - DB_PATH=/data/drinks.db
    volumes:
      - sqlite_data:/data
    ports:
      - "3421:8000"

volumes:
  sqlite_data: